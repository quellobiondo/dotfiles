- defaults:
    link:
      create: true
      relink: true
- clean: ['~']
- link:
    ~/.tmux.conf:
      path: tmux.conf
    ~/.vimrc:
      path: vimrc
    ~/.gitconfig:
      path: gitconfig
- shell:
  - git submodule sync --recursive
  - git submodule update
