- defaults:
    link:
      create: true
      relink: true
- clean: ['~']
- link:
    ~/.tmux.conf:
      path: tmux.conf
    ~/.vim: vim
    ~/.vimrc:
      path: vimrc
- shell:
  - git submodule sync --recursive
  - git submodule update
